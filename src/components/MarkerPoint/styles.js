import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    Marker: {
        height: 45,
        width: 45,
    },
    Code: {
        justifyContent: 'center',
        alignItems: 'center',
        padding: 100
    },
    container: {
        padding: 25,
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    closeText: {
        fontSize: 24,
        color: '#00479e',
        textAlign: 'center',
        padding: 50
    }
});